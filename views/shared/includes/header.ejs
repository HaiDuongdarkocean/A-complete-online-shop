<header>
    <div><a href="/">TheOcean</a></div>
    <nav>
        <ui>
            <% if (locals.isAuth) { %>
            <li>
                <form action="/logout" method="POST">
                    <input type="hidden" name="_csrf" value="<%= locals.csrfToken %>">
                    <button>Logout</button>
                </form>
            </li>
            <% } %>
        </ui>
    </nav>
</header>